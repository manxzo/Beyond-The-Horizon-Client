// User role enum
export enum UserRole {
  Member = "member",
  Sponsor = "sponsor",
  Admin = "admin",
}

// Application status enum
export enum ApplicationStatus {
  Pending = "pending",
  Approved = "approved",
  Rejected = "rejected",
}
